# Localization Manager

Современное WPF приложение для перевода файлов локализации через Google Translate API.

## Требования

- .NET 8.0 SDK или выше
- Google Cloud API Key с включенным Google Translate API
- Visual Studio 2022 или выше (или другой редактор с поддержкой .NET)

## Сборка проекта

1. Откройте терминал в папке проекта
2. Выполните команду:
   ```bash
   dotnet restore
   dotnet build
   ```
3. Для запуска:
   ```bash
   dotnet run
   ```
4. Или создайте исполняемый файл:
   ```bash
   dotnet publish -c Release
   ```

## Получение API ключа

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите "Cloud Translation API"
4. Перейдите в "Credentials" → "Create Credentials" → "API Key"
5. Скопируйте сгенерированный API ключ

> 💡 **Подсказка**: В приложении есть встроенная подсказка (иконка ℹ️) рядом с полем API ключа с подробными инструкциями.

## Использование

1. Запустите приложение
2. Введите ваш Google Translate API ключ и нажмите "Test Connection" для проверки
3. Выберите исходный файл локализации (например, `localization.json`) через кнопку "Browse..."
4. Выберите путь для сохранения переведенного файла через кнопку "Browse..."
5. Выберите целевой язык перевода из выпадающего списка
6. Нажмите "🚀 Start Translation" для начала процесса перевода

## Особенности

- 🎨 Современный и интуитивный интерфейс
- 🔑 Встроенные подсказки по получению API ключа
- 📊 Отображение прогресса перевода в реальном времени с процентами
- 🔄 Автоматически пропускает служебные ключи (начинающиеся с `__`)
- 💾 Сохраняет структуру JSON файла
- 🌍 Обновляет код языка в поле `__LANG`
- ⚡ Обработка ошибок с продолжением работы при сбоях отдельных переводов

## Поддерживаемые языки

- Russian (ru)
- Ukrainian (uk)
- Polish (pl)
- German (de)
- French (fr)
- Spanish (es)
- Italian (it)
- Portuguese (pt)
- Chinese (zh)
- Japanese (ja)
- Korean (ko)

## Примечания

- Процесс перевода может занять значительное время для больших файлов
- Убедитесь, что у вас достаточно квоты в Google Cloud для перевода
- Рекомендуется делать резервные копии исходных файлов

