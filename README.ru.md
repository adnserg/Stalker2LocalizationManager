# Localization Manager

Современное WPF приложение для перевода файлов локализации с поддержкой бесплатного и платного вариантов перевода.

## Требования

- .NET 8.0 SDK или выше
- Visual Studio 2022 или выше (или другой редактор с поддержкой .NET)
- Для Google Translate API: Google Cloud API Key с включенным Google Translate API (опционально)

## Сборка проекта

1. Откройте терминал в папке проекта
2. Выполните команду:
   ```bash
   dotnet restore
   dotnet build
   ```
3. Для запуска:
   ```bash
   dotnet run
   ```
4. Или создайте исполняемый файл:
   ```bash
   dotnet publish -c Release
   ```

## Выбор провайдера перевода

Приложение поддерживает два варианта:

### 🆓 LibreTranslate (Бесплатный)
- Полностью бесплатный и открытый исходный код
- Не требует API ключа
- Использует публичные серверы перевода
- Автоматически тестируется при выборе

### 💰 Google Translate API (Платный)
- Требует API ключ от Google Cloud
- Более высокая точность перевода
- Требует настройки в Google Cloud Console

### Получение Google Translate API ключа

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите "Cloud Translation API"
4. Перейдите в "Credentials" → "Create Credentials" → "API Key"
5. Скопируйте сгенерированный API ключ

> 💡 **Подсказка**: В приложении есть встроенная подсказка (иконка ℹ️) рядом с полем API ключа с подробными инструкциями.

## Использование

1. Запустите приложение
2. **Выберите провайдера перевода:**
   - **LibreTranslate (Free)** - выбран по умолчанию, готов к использованию без настройки
   - **Google Translate API (Paid)** - введите API ключ и нажмите "Test Connection"
3. Выберите исходный файл локализации (например, `localization.json`) через кнопку "Browse..."
4. Выберите путь для сохранения переведенного файла через кнопку "Browse..."
5. Выберите целевой язык перевода из выпадающего списка
6. Нажмите "🚀 Start Translation" для начала процесса перевода

## Особенности

- 🎨 Современный и интуитивный интерфейс
- 🆓 **Бесплатный вариант перевода через LibreTranslate** - не требует API ключа
- 💰 Опциональная поддержка Google Translate API для более точного перевода
- 🔑 Встроенные подсказки по получению API ключа
- 📊 Отображение прогресса перевода в реальном времени с процентами
- 🔄 Автоматически пропускает служебные ключи (начинающиеся с `__`)
- 💾 Сохраняет структуру JSON файла
- 🌍 Обновляет код языка в поле `__LANG`
- ⚡ Обработка ошибок с продолжением работы при сбоях отдельных переводов

## Поддерживаемые языки

- Russian (ru)
- Ukrainian (uk)
- Polish (pl)
- German (de)
- French (fr)
- Spanish (es)
- Italian (it)
- Portuguese (pt)
- Chinese (zh)
- Japanese (ja)
- Korean (ko)

## Примечания

- Процесс перевода может занять значительное время для больших файлов
- **LibreTranslate**: Бесплатный, но может быть медленнее и менее точным для некоторых языков
- **Google Translate API**: Требует настройки и может иметь лимиты/плату, но обычно более точный
- Рекомендуется делать резервные копии исходных файлов
- Для больших файлов рекомендуется использовать Google Translate API из-за лучшей производительности

